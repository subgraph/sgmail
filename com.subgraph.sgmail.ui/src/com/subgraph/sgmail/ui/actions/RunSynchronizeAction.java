package com.subgraph.sgmail.ui.actions;

import org.eclipse.jface.action.Action;
import org.eclipse.jface.action.IAction;

public class RunSynchronizeAction extends Action {
	
	public RunSynchronizeAction() {
		super("Synchronize Accounts", IAction.AS_CHECK_BOX);
	}

}
